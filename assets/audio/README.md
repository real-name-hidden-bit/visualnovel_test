# Audio

For R5. Recommended files:

- `bgm_mystery.mp3`   — looping ambient/background music
- `sfx_thunder.mp3`   — key-moment sound effect
- `sfx_door.mp3`      — door creak / discovery
- `sfx_heartbeat.mp3` — tension cue

Played via the `audioplayers` package.
